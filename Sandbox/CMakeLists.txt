set(this SandBox)
ucm_add_dirs(Src TO sources RECURSIVE)
add_executable(${this} ${sources})
target_link_libraries(${this} PRIVATE Spark)
set_property(TARGET ${this} PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")